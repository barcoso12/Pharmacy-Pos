{
    "buildCommand": "npx turbo run build --filter=@pos/web",
    "outputDirectory": "apps/web/dist",
    "installCommand": "npm install",
    "framework": null,
    "devCommand": "npx turbo run dev --filter=@pos/web"
}